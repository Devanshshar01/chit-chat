/**
 * Values only you can fill in - none of these have safe defaults.
 * Get a Giphy key at https://developers.giphy.com/ (free, instant).
 */
export const GIPHY_API_KEY = "";

/**
 * Where the backend is reachable FROM THE DEVICE running the app:
 *   - Android emulator talking to a backend on the host machine: http://10.0.2.2:8000
 *   - physical device on the same LAN: http://<your-machine-LAN-IP>:8000
 *   - production: your deployed https:// URL
 */
export const API_BASE_URL = "http://192.168.1.101:8000";
